toolName: todo_write
            
status: success
          
Todos updated: 9 items
          

        
            
toolName: view_files
            
status: success
          
            
filePath: D:\Documents\WEBSITE\ikaunimed-9.or.id\app\Http\Controllers\AlumniDirectoryController.php
          

        
            
toolName: apply_patch
            
status: failed
          
          

        
            
toolName: edit_file_update
            
status: success
          
            
filePath: D:\Documents\WEBSITE\ikaunimed-9.or.id\app\Http\Controllers\AlumniDirectoryController.php
          
changes: 
          

        
            
toolName: todo_write
            
status: success
          
Todos updated: 9 items
          

        
            
toolName: view_files
            
status: success
          
            
filePath: D:\Documents\WEBSITE\ikaunimed-9.or.id\resources\js\Pages\Alumni\Directory\Index.tsx
          

        
            
toolName: apply_patch
            
status: failed
          
          

        
            
toolName: edit_file_update
            
status: success
          
            
filePath: D:\Documents\WEBSITE\ikaunimed-9.or.id\resources\js\Pages\Alumni\Directory\Index.tsx
          
changes: 
          

        
            
toolName: GetDiagnostics
            
status: success
          
          

        
            
toolName: todo_write
            
status: success
          
Todos updated: 9 items
          
Tahap 4 sudah saya eksekusi sesuai blueprint di `11._diskusi_tahap_4.md`: direktori `/alumni` sekarang punya filter horizontal (nama, bidang, status, angkatan) yang konsisten dengan kontrak domain dan aman dari sisi query.

Berikut detailnya.

**1️⃣ Backend: Filter & Query di Controller**

File: [app/Http/Controllers/AlumniDirectoryController.php](file:///D:/Documents/WEBSITE/ikaunimed-9.or.id/app/Http/Controllers/AlumniDirectoryController.php)

Bagian `index` saya kembangkan dari sekadar listing menjadi listing + filter:

```php
public function index(Request $request)
{
    $query = User::appearInDirectory()
        ->with('educations');

    $query->when($request->search, function ($q, $search) {
        $q->where('name', 'like', '%' . $search . '%');
    });

    $query->when($request->bidang_pekerjaan, function ($q, $bidang) {
        $q->where('bidang_pekerjaan', $bidang);
    });

    $query->when($request->status_pekerjaan, function ($q, $status) {
        $q->where('status_pekerjaan', $status);
    });

    $query->when($request->angkatan, function ($q, $angkatan) {
        $q->whereHas('educations', function ($sub) use ($angkatan) {
            $sub->where('admission_year', $angkatan);
        });
    });

    $query->latest('updated_at');

    $users = $query->paginate(12)->withQueryString();

    $alumni = $users->getCollection()->map(function (User $user) {
        $educationLevels = ['D1', 'D2', 'D3', 'D4', 'S1', 'S2', 'S3'];

        $primaryEducation = $user->educations
            ->filter(function ($edu) {
                return $edu->major && $edu->admission_year;
            })
            ->sortBy(function ($edu) use ($educationLevels) {
                $index = array_search($edu->level, $educationLevels);
                return $index === false ? -1 : $index;
            })
            ->last();

        return [
            'id' => $user->id,
            'name' => $user->name,
            'bidang_pekerjaan' => $user->bidang_pekerjaan,
            'posisi_saat_ini' => $user->posisi_saat_ini,
            'perusahaan' => $user->perusahaan,
            'kota_profesional' => $user->kota_profesional,
            'status_pekerjaan' => $user->status_pekerjaan,
            'profile_level' => $user->profile_level,
            'profile_completion_score' => $user->profile_completion_score,
            'primary_education' => $primaryEducation ? [
                'level' => $primaryEducation->level,
                'major' => $primaryEducation->major,
                'admission_year' => $primaryEducation->admission_year,
                'graduation_year' => $primaryEducation->graduation_year,
            ] : null,
        ];
    });

    $users->setCollection($alumni);

    return Inertia::render('Alumni/Directory/Index', [
        'alumni' => $users,
        'filters' => $request->only([
            'search',
            'bidang_pekerjaan',
            'status_pekerjaan',
            'angkatan',
        ]),
    ]);
}
```

Poin penting:

- Tetap memakai `User::appearInDirectory()` → **privacy + level rule** tetap dijaga.
- Filter yang ditambahkan (semua opsional):
  - `search` → `name LIKE %…%`.
  - `bidang_pekerjaan` → equality, cocok dengan enum yang sudah kita pakai di profil.
  - `status_pekerjaan` → equality, cocok dengan enum (tetap/kontrak/wirausaha/…).
  - `angkatan` → `whereHas('educations', admission_year = angkatan)`, hanya dieksekusi kalau filter ini diisi, sehingga tidak menambah beban query ketika tidak dipakai.
- Tetap `latest('updated_at')` untuk urutan, lalu `paginate(12)->withQueryString()` supaya pindah halaman tidak menghapus filter.
- Inertia sekarang mengirimkan juga `filters` ke frontend agar nilai filter bisa dipertahankan di UI.

Struktur transformasi data (`$alumni`) tidak berubah dari Tahap 3, jadi kontrak data ke frontend tetap stabil.

**2️⃣ Frontend: Filter Horizontal di /alumni**

File: [resources/js/Pages/Alumni/Directory/Index.tsx](file:///D:/Documents/WEBSITE/ikaunimed-9.or.id/resources/js/Pages/Alumni/Directory/Index.tsx)

Perubahan utama:

1. **Props dan State Filter**

   ```ts
   import React, { useState } from 'react';
   import { Head, Link, usePage, router } from '@inertiajs/react';
   ...

   interface Props {
     alumni: PaginatedAlumni;
     filters: {
       search?: string;
       bidang_pekerjaan?: string;
       status_pekerjaan?: string;
       angkatan?: string;
     };
   }

   export default function Index({ alumni, filters }: Props) {
     const { auth }: any = usePage().props;

     const [search, setSearch] = useState(filters.search || '');
     const [bidang, setBidang] = useState(filters.bidang_pekerjaan || '');
     const [status, setStatus] = useState(filters.status_pekerjaan || '');
     const [angkatan, setAngkatan] = useState(filters.angkatan || '');
   ```

2. **Handler submit & reset**

   ```ts
   const handleFilterSubmit = (e: React.FormEvent) => {
     e.preventDefault();
     router.get(
       route('alumni.directory'),
       {
         search,
         bidang_pekerjaan: bidang,
         status_pekerjaan: status,
         angkatan,
       },
       {
         preserveState: true,
         preserveScroll: true,
       }
     );
   };

   const handleReset = () => {
     setSearch('');
     setBidang('');
     setStatus('');
     setAngkatan('');
     router.get(route('alumni.directory'), {}, { preserveState: false, preserveScroll: true });
   };
   ```

3. **Opsi filter (mengikuti enum yang sudah ada)**

   ```ts
   const bidangOptions = [
     'Pendidikan',
     'Pemerintahan',
     'Swasta',
     'Wirausaha',
     'BUMN/BUMD',
     'Non-Profit/LSM',
     'Freelance/Konsultan',
     'Teknologi Informasi',
     'Kreatif/Media',
     'Kesehatan',
     'Industri/Manufaktur',
     'Lainnya',
   ];

   const statusOptions = [
     { value: 'tetap', label: 'Tetap' },
     { value: 'kontrak', label: 'Kontrak' },
     { value: 'wirausaha', label: 'Wirausaha' },
     { value: 'freelancer', label: 'Freelancer' },
     { value: 'studi_lanjut', label: 'Studi Lanjut' },
     { value: 'mencari_kerja', label: 'Open to Work' },
   ];
   ```

4. **Bar filter horizontal di atas grid**

   Tepat setelah container utama:

   ```tsx
   <div className="container mx-auto px-4 py-12">
     <div className="bg-white rounded-lg border border-gray-200 shadow-sm p-4 mb-8">
       <form
         onSubmit={handleFilterSubmit}
         className="flex flex-col lg:flex-row gap-4 items-stretch lg:items-end"
       >
         <div className="flex-1">
           <label className="block text-xs font-semibold text-gray-600 mb-1">
             Cari nama alumni
           </label>
           <input
             type="text"
             value={search}
             onChange={(e) => setSearch(e.target.value)}
             placeholder="Ketik nama alumni..."
             className="w-full border-gray-300 rounded-md text-sm focus:ring-sky-500 focus:border-sky-500"
           />
         </div>

         <div className="w-full lg:w-56">
           <label className="block text-xs font-semibold text-gray-600 mb-1">
             Bidang pekerjaan
           </label>
           <select
             value={bidang}
             onChange={(e) => setBidang(e.target.value)}
             className="w-full border-gray-300 rounded-md text-sm focus:ring-sky-500 focus:border-sky-500"
           >
             <option value="">Semua bidang</option>
             {bidangOptions.map((b) => (
               <option key={b} value={b}>
                 {b}
               </option>
             ))}
           </select>
         </div>

         <div className="w-full lg:w-48">
           <label className="block text-xs font-semibold text-gray-600 mb-1">
             Status pekerjaan
           </label>
           <select
             value={status}
             onChange={(e) => setStatus(e.target.value)}
             className="w-full border-gray-300 rounded-md text-sm focus:ring-sky-500 focus:border-sky-500"
           >
             <option value="">Semua status</option>
             {statusOptions.map((s) => (
               <option key={s.value} value={s.value}>
                 {s.label}
               </option>
             ))}
           </select>
         </div>

         <div className="w-full lg:w-36">
           <label className="block text-xs font-semibold text-gray-600 mb-1">
             Angkatan
           </label>
           <input
             type="number"
             value={angkatan}
             onChange={(e) => setAngkatan(e.target.value)}
             placeholder="2010"
             className="w-full border-gray-300 rounded-md text-sm focus:ring-sky-500 focus:border-sky-500"
           />
         </div>

         <div className="flex gap-2">
           <button
             type="submit"
             className="inline-flex items-center justify-center px-4 py-2 rounded-md bg-sky-600 text-white text-sm font-semibold hover:bg-sky-700 transition-colors"
           >
             Terapkan
           </button>
           <button
             type="button"
             onClick={handleReset}
             className="inline-flex items-center justify-center px-3 py-2 rounded-md border border-gray-300 text-xs font-medium text-gray-700 hover:bg-gray-50"
           >
             Reset
           </button>
         </div>
       </form>
     </div>
   ```

Setelah bar filter, struktur grid kartu alumni dan pagination **tidak diubah**, jadi semua perilaku Tahap 3 tetap sama, hanya sekarang bisa difokuskan lewat filter.

**3️⃣ Konsistensi dengan Diskusi Tahap 4**

Dibandingkan dengan rangkuman Anda di `11._diskusi_tahap_4.md`:

- **Filter prioritas 1** (nama, bidang, status) → sudah terpasang.
- **Filter angkatan** → sudah ada input angka, di-backend pakai `whereHas('educations')` yang hanya aktif jika user isi angkatan.
- **Strategi query**:
  - `when(...)` untuk setiap filter.
  - `with('educations')` tetap dipakai untuk menghindari N+1.
  - `paginate()->withQueryString()` menjaga filter saat paging.
- **UX**:
  - Bentuk filter horizontal di atas grid, konsisten dengan pola yang kita bahas (versi 1.5).
  - Ada tombol “Reset” yang clear kalau user ingin kembali ke listing penuh.

Diagnostik project (VS Code diagnostics) setelah perubahan tidak menunjukkan error baru.

---

Jika Anda mau, langkah berikutnya bisa:

- Uji manual di browser:
  - `/alumni` tanpa filter.
  - `/alumni?bidang_pekerjaan=Pendidikan`.
  - `/alumni?status_pekerjaan=mencari_kerja`.
  - Kombinasi search + angkatan.
- Lalu kita bisa diskusikan lagi Tahap 4B (integrasi lowongan → profil alumni) sebagai langkah selanjutnya.

---------------------------------------------------------

Saya sudah membaca rangkuman Tahap 4 Anda. Secara arsitektur dan disiplin domain, ini **rapi dan matang**. 👍
Anda tidak hanya “menambah filter”, tapi menjaga:

* Kontrak data tetap stabil (tidak merusak Tahap 3)
* Privacy rule tetap lewat `appearInDirectory()`
* Query tetap kondisional (`when()`)
* Pagination tetap sinkron (`withQueryString()`)

Itu tanda eksekusi yang sadar desain, bukan sekadar tambal fitur.

---

# 📌 Pendapat Strategis atas Tahap 4

## ✅ 1. Dari sisi Domain

Filter:

* `search`
* `bidang_pekerjaan`
* `status_pekerjaan`
* `angkatan (whereHas)`

Ini sudah mencerminkan 4 dimensi utama alumni:

* Identitas (nama)
* Karier (bidang)
* Status profesional
* Angkatan (relasi pendidikan)

Artinya direktori sekarang sudah “navigable secara bisnis”.

Ini bukan lagi list → ini sudah **alat eksplorasi jaringan alumni**.

---

## ✅ 2. Dari sisi Query Performance

Anda sudah benar:

* `when()` → query hanya aktif saat dibutuhkan
* `whereHas()` hanya jalan jika ada `angkatan`
* eager load `educations`
* pagination 12

Ini aman untuk skala awal–menengah.

Namun…

Jika data alumni nanti > 5.000–10.000 record, search `LIKE %nama%` akan mulai berat.

Tapi itu bukan masalah sekarang.

---

## ✅ 3. Dari sisi UX

Filter horizontal di atas grid adalah pilihan yang tepat.

Kenapa?
Karena:

* Ini bukan marketplace dengan 20 filter.
* Ini direktori relasi.
* Kita ingin cepat & ringan.

Tombol Reset juga tepat.

---

# 🎯 Pertanyaan Penting: Sekarang Mau Arahkan ke Mana?

Tahap 4 menyelesaikan "Browse Alumni".

Sekarang ada 3 arah strategis besar.

---

# 🚀 OPSI LANGKAH SELANJUTNYA

## 🔵 OPSI A — Tahap 4B: Integrasi Lowongan ↔ Alumni (Rekomendasi)

Ini yang paling logis.

Kenapa?

Karena sekarang:

* Anda sudah bisa filter alumni berdasarkan status pekerjaan.
* Anda tahu siapa yang "Open to Work".

Langkah berikutnya secara natural adalah:

> Menghubungkan lowongan dengan alumni yang relevan.

### Contoh Eksekusi 4B:

1. Di halaman Lowongan → tampilkan:

   * "Alumni yang Open to Work (X orang)"
2. Atau:

   * Alumni bisa klik “Tertarik” pada lowongan.
3. Atau:

   * Sistem rekomendasikan lowongan berdasarkan bidang.

Ini akan mengubah website dari:
Direktori statis → menjadi ekosistem profesional.

Saya sangat menyarankan arah ini.

---

## 🟡 OPSI B — Optimasi Performa & Indexing (Teknis)

Jika ingin rapi dulu sebelum lanjut fitur:

Tambahkan index database:

```sql
INDEX name
INDEX bidang_pekerjaan
INDEX status_pekerjaan
INDEX updated_at
INDEX admission_year (di tabel educations)
```

Kenapa?
Karena filter Anda sudah production-grade.
Sekarang tinggal buat dia scalable.

Ini langkah “engine room”, bukan fitur.

---

## 🟢 OPSI C — Naikkan Level Direktori (Fitur Premium)

Beberapa ide:

* Badge “Top Profile”
* Sorting:

  * Paling lengkap
  * Terbaru update
  * Paling aktif
* Hitung statistik di atas:

  * Total Alumni
  * % Open to Work
  * % Wirausaha

Ini membuat direktori terasa hidup.

---

# 🧠 Saran Saya (Paling Rasional)

Urutannya sebaiknya:

1️⃣ Tahap 4B – Integrasi Lowongan ↔ Alumni
2️⃣ Tambah index database
3️⃣ Tambah statistik ringkas di atas direktori

Jangan lompat ke fitur kosmetik dulu.

---

# 🔥 Evaluasi Jujur

Tahap 4 adalah titik di mana:
Website Anda mulai terasa seperti platform.

Bukan lagi:
"Website organisasi dengan CMS"

Tapi sudah mengarah ke:
"Professional Alumni Network System"

Dan itu perubahan level.

---

# ❓ Pertanyaan untuk Anda

Sekarang saya ingin tahu:

Apakah tujuan akhir Anda adalah:
A) Direktori informatif
B) Platform koneksi profesional
C) Marketplace karier alumni
D) Portal organisasi dengan fitur tambahan

Jawaban itu akan menentukan Tahap 5.

Kalau Anda mau, saya bisa langsung gambarkan Blueprint Tahap 5 sesuai arah yang Anda pilih.

----------------------------------------------------------------

toolName: view_files
            
status: success
          
            
filePath: D:\Documents\WEBSITE\ikaunimed-9.or.id\documentations\08._program_development\12._eksekusi_tahap_4.md
          

        
            
toolName: todo_write
            
status: success
          
Todos updated: 11 items
          

        
            
toolName: view_files
            
status: success
          
            
filePath: D:\Documents\WEBSITE\ikaunimed-9.or.id\app\Http\Controllers\JobVacancyController.php
          

        
            
toolName: apply_patch
            
status: failed
          
          

        
            
toolName: edit_file_update
            
status: success
          
            
filePath: D:\Documents\WEBSITE\ikaunimed-9.or.id\app\Http\Controllers\JobVacancyController.php
          
changes: 
          

        
            
toolName: todo_write
            
status: success
          
Todos updated: 11 items
          

        
            
toolName: view_files
            
status: success
          
            
filePath: D:\Documents\WEBSITE\ikaunimed-9.or.id\resources\js\Pages\JobVacancy\Index.tsx
          

        
            
toolName: apply_patch
            
status: failed
          
          

        
            
toolName: edit_file_update
            
status: success
          
            
filePath: D:\Documents\WEBSITE\ikaunimed-9.or.id\resources\js\Pages\JobVacancy\Index.tsx
          
changes: 
          

        
            
toolName: GetDiagnostics
            
status: success
          
          

        
            
toolName: todo_write
            
status: success
          
Todos updated: 11 items
          
Saya sudah baca rangkuman di `12._eksekusi_tahap_4.md`. Tahap 4 yang ada di sana (filter direktori + analisis lanjutan) sudah sejalan dengan struktur kode sekarang, jadi saya lanjutkan Tahap 4 dengan sub-langkah 4B yang paling rasional: integrasi Lowongan ↔ Direktori Alumni, tanpa mengubah pola arsitektur yang sudah kita bangun.

Berikut yang saya kerjakan dan di mana lokasinya.

---

**1️⃣ Integrasi Backend: JobVacancyController ↔ Data Alumni**

File: [app/Http/Controllers/JobVacancyController.php](file:///D:/Documents/WEBSITE/ikaunimed-9.or.id/app/Http/Controllers/JobVacancyController.php)

Saya tambahkan:

- Import `User` dan penggunaan scope `appearInDirectory()` agar tetap patuh kontrak domain.

Sebelum:

```php
use App\Models\JobVacancy;
use Illuminate\Http\Request;
use Inertia\Inertia;
```

Sesudah:

```php
use App\Models\JobVacancy;
use App\Models\User;
use Illuminate\Http\Request;
use Inertia\Inertia;
```

Di method `index`, saya integrasikan statistik alumni:

```php
public function index(Request $request)
{
    $query = JobVacancy::query()
        ->where('status', 'active')
        ->when($request->search, function ($q, $search) {
            $q->where(function ($sub) use ($search) {
                $sub->where('title', 'like', "%{$search}%")
                    ->orWhere('company', 'like', "%{$search}%")
                    ->orWhere('location', 'like', "%{$search}%");
            });
        })
        ->when($request->type, function ($q, $type) {
            $q->where('type', $type);
        })
        ->latest();

    $alumniBaseQuery = User::appearInDirectory();

    $alumniStats = [
        'total' => (clone $alumniBaseQuery)->count(),
        'open_to_work' => (clone $alumniBaseQuery)
            ->where('status_pekerjaan', 'mencari_kerja')
            ->count(),
    ];

    return Inertia::render('JobVacancy/Index', [
        'vacancies' => $query->paginate(9)->withQueryString(),
        'filters' => $request->only(['search', 'type']),
        'alumniStats' => $alumniStats,
    ]);
}
```

Catatan penting:

- Tetap memakai `User::appearInDirectory()` → hanya alumni yang:
  - `public_profile = true`, dan
  - `profile_level >= 1`.
- Statistik yang dihitung:
  - `total` → total alumni yang layak tampil di direktori.
  - `open_to_work` → alumni yang layak tampil dan `status_pekerjaan = 'mencari_kerja'`.
- Menggunakan `(clone $alumniBaseQuery)` agar query base tidak tercemar saat menambah where untuk open_to_work.

Ini sejalan dengan analisis di dokumen: integrasi dilakukan lewat domain yang sudah “rapi”, bukan query liar baru.

---

**2️⃣ Integrasi Frontend: Banner “Alumni Open to Work” di /lowongan-kerja**

File: [resources/js/Pages/JobVacancy/Index.tsx](file:///D:/Documents/WEBSITE/ikaunimed-9.or.id/resources/js/Pages/JobVacancy/Index.tsx)

Saya perluas props supaya halaman lowongan bisa menerima statistik alumni:

Sebelum:

```ts
interface Props {
  vacancies: {
    data: JobVacancy[];
    links: any[];
  };
  filters: {
    search?: string;
    type?: string;
  };
}

export default function Index({ vacancies, filters }: Props) {
```

Sesudah:

```ts
interface Props {
  vacancies: {
    data: JobVacancy[];
    links: any[];
  };
  filters: {
    search?: string;
    type?: string;
  };
  alumniStats?: {
    total: number;
    open_to_work: number;
  };
}

export default function Index({ vacancies, filters, alumniStats }: Props) {
```

Lalu, tepat setelah hero hijau (section judul “Temukan Karir Impianmu”), saya tambah satu bar informasi yang menghubungkan lowongan dengan direktori alumni:

```tsx
{alumniStats && (
  <div className="bg-emerald-50 border-b border-emerald-100">
    <div className="container mx-auto px-4 py-4 flex flex-col md:flex-row md:items-center md:justify-between gap-2 text-sm">
      <div className="text-emerald-900">
        {alumniStats.open_to_work > 0 ? (
          <>
            <span className="font-semibold">{alumniStats.open_to_work}</span> alumni IKA UNIMED saat ini menandai
            status <span className="font-semibold">Open to Work</span>.
          </>
        ) : (
          <>
            Alumni IKA UNIMED aktif mengisi profil profesional mereka di direktori.
          </>
        )}
      </div>
      <Link
        href={route('alumni.directory', { status_pekerjaan: 'mencari_kerja' })}
        className="inline-flex items-center justify-center px-3 py-1.5 rounded-md bg-emerald-600 text-white font-medium hover:bg-emerald-700 transition-colors text-xs md:text-sm"
      >
        Lihat Alumni Open to Work
      </Link>
    </div>
  </div>
)}
```

Dampaknya:

- Di atas daftar lowongan, user langsung melihat bahwa:
  - Ada X alumni “Open to Work” (kalau ada).
  - Selalu ada CTA yang mengarah ke `/alumni` dengan filter `status_pekerjaan=mencari_kerja`.
- Secara arsitektur:
  - Halaman lowongan tetap memakai layout yang sama.
  - Tidak ada perubahan pada struktur search/filter job yang sudah ada.
  - Integrasi dilakukan lewat satu props tambahan `alumniStats`, yang sumber datanya tetap dari domain `User::appearInDirectory()`.

---

**3️⃣ Konsistensi dengan Struktur dan Blueprint**

Rangkuman di `12._eksekusi_tahap_4.md` sudah menggambarkan:

- Tahap 4A: filter direktori → sudah kita kerjakan sebelumnya.
- Langkah lanjutan yang Anda tulis di sana (4B integrasi lowongan ↔ alumni) saya terjemahkan minimalis tapi fungsional:
  - Bukan langsung fitur kompleks (apply, rekomendasi otomatis, dsb.).
  - Namun sudah memenuhi ide “lowongan terhubung dengan alumni Open to Work”:
    - Ada statistik live di `/lowongan-kerja`.
    - Ada link langsung ke direktori alumni dengan filter status.

Struktur besar yang sudah kita bangun tidak diubah:

- Domain:
  - `User::appearInDirectory()` tetap jadi pintu utama data publik alumni.
- Application:
  - `AlumniDirectoryController@index` dan `JobVacancyController@index` sama-sama hanya mengonsumsi rule domain + menerapkan filter tambahan.
- Presentation:
  - Halaman `/alumni` tetap fokus kartu alumni + filter.
  - Halaman `/lowongan-kerja` tetap fokus daftar lowongan, sekarang dengan satu jembatan kecil ke direktori.

Diagnostik (GetDiagnostics) setelah perubahan juga bersih.

---

Kalau Anda setuju, Tahap 4 bisa kita anggap selesai dengan cakupan:

- 4A: Filter & sorting direktori alumni.
- 4B: Integrasi awal lowongan ↔ alumni melalui statistik dan link “Alumni Open to Work” di halaman lowongan.

Langkah alami berikutnya (Tahap 5) bisa mengikuti opsi strategis di dokumen: misalnya memperkuat sisi “platform koneksi profesional” (rekomendasi lowongan, badge profil, statistik agregat, dsb.).